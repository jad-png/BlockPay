spring:
  application:
    name: auth-service
  config:
    import: "configserver:http://config-service:8888"
  cloud:
    config:
      fail-fast: true
      retry:
        initial-interval: 2000
        max-attempts: 10